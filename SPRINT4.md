# اسپرینت ۴ — سبد خرید و ارسال

همه مسیرها به `Authorization: Bearer <access>` نیاز دارند. مبالغ با همان واحد
قیمت محصول و به صورت رشته اعشاری برگردانده می‌شوند؛ تبدیل ریال/تومان انجام نمی‌شود.

| متد و مسیر | ورودی | پاسخ موفق |
| --- | --- | --- |
| GET /api/cart/ | ندارد | 200، سبد جاری |
| PATCH /api/cart/ | `{"city":"تهران"}` | 200، سبد با هزینه جدید |
| POST /api/cart/items/ | `{"product":1,"quantity":2}` | 201 برای قلم جدید، 200 برای افزایش قلم موجود |
| PATCH /api/cart/items/{id}/ | `{"quantity":1}` | 200، سبد؛ تعداد جایگزین می‌شود |
| DELETE /api/cart/items/{id}/ | ندارد | 204 بدون بدنه |
| GET /api/shipping/cities/ | page اختیاری | 200، فهرست صفحه‌بندی‌شده نرخ‌ها |

نمونه پاسخ سبد:

```json
{
  "id": 1,
  "city": "تهران",
  "items": [{"id": 1, "product": 1, "name": "عطر", "quantity": 2,
             "unit_price": "90.00", "line_total": "180.00"}],
  "subtotal": "180.00",
  "shipping_cost": "20.00",
  "total": "200.00"
}
```

نرخ‌ها در پنل admin، بخش Shipping rates ثبت و ویرایش می‌شوند.
شهر ناشناخته با 400 رد می‌شود؛ قبل از انتخاب شهر، city و shipping_cost و total
برابر null هستند. subtotal همیشه قابل محاسبه است. شهر استفاده‌شده در سبد قابل حذف نیست.
POST تکراری تعداد را افزایش می‌دهد؛ PATCH تعداد نهایی را تعیین می‌کند.
تعداد باید عدد صحیح مثبت و حداکثر برابر موجودی باشد. محصول غیرفعال قابل افزودن نیست.
قلم کاربر دیگر و محصول ناموجود 404 می‌گیرند. درخواست بدون توکن 401 می‌گیرد.
موجودی در این اسپرینت کم نمی‌شود و کالا رزرو نمی‌شود؛ هنگام سفارش باید دوباره کنترل شود.
مبالغ از قیمت جاری محصول محاسبه می‌شوند و قیمت نهایی محصول تا دو رقم اعشار گرد می‌شود.

تست مستقل از دیتابیس توسعه:

```powershell
python manage.py test --settings=config.test_settings
```

تست‌ها روی SQLite در حافظه هستند؛ قفل‌های هم‌زمانی PostgreSQL با این تست‌ها تأیید نمی‌شوند.
برای محیطی با تاریخچه migration سالم، migrationهای محصولات و سبد باید قبل از استفاده
با `python manage.py migrate` اعمال شوند. داده یا تاریخچه دیتابیس ناسازگار را حذف نکنید.
